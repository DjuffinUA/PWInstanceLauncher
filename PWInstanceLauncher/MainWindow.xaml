<Window x:Class="PWInstanceLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PW Instance Launcher"
        Height="800" Width="720" Icon="/PWInstanceLauncher.png">
    <Window.Background>
        <ImageBrush ImageSource="/gragon.png" Stretch="UniformToFill"/>
    </Window.Background>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Game Path:" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{Binding GamePathDisplay}" TextWrapping="Wrap" Margin="0,2,0,6" Foreground="Black" HorizontalAlignment="Center" TextAlignment="Center"/>
        <Button Grid.Row="2"
                Content="Change Game Path"
                Command="{Binding ChangeGamePathCommand}"
                HorizontalAlignment="Stretch"
                Margin="0,0,0,10"
                Background="#FFFAFBFB"/>

        <TextBlock Grid.Row="3" Text="Launch Mode:" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,4"/>

        <ComboBox Grid.Row="4"
                  Margin="0,0,0,10"
                  HorizontalAlignment="Stretch"
                  ItemsSource="{Binding LaunchModes}"
                  SelectedItem="{Binding Config.LaunchMode, Mode=TwoWay}"/>

        <Grid Grid.Row="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0"
                    Content="Add Character"
                    Command="{Binding AddCharacterCommand}"
                    HorizontalAlignment="Stretch"
                    Margin="0,0,0,10"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Characters}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2.8*"/>
                                    <ColumnDefinition Width="1.4*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1.4*"/>
                                    <ColumnDefinition Width="1.8*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Border Width="64" Height="64" CornerRadius="32" ClipToBounds="True" Margin="0,0,8,0">
                                        <Image Source="{Binding ImagePath}" Width="64" Height="64" Stretch="Fill"/>
                                    </Border>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" FontWeight="Bold" Foreground="White" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="Launch"
                                        Command="{Binding DataContext.LaunchCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center" Width="70"/>
                                <Button Grid.Column="2" Content="Edit" Margin="5,0,0,0"
                                        Command="{Binding DataContext.EditCharacterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center" Width="50"/>
                                <Button Grid.Column="3" Content="Remove" Margin="5,0,0,0"
                                        Command="{Binding DataContext.RemoveCharacterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center" Width="70"/>
                                <TextBlock Grid.Column="4"
                                           Margin="8,0,0,0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           FontWeight="SemiBold"
                                           Text="{Binding RuntimeStatus}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Separator Grid.Row="6" Margin="0,8,0,8"/>
        <TextBlock Grid.Row="7" Text="Status:" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center"/>
        <TextBlock Grid.Row="8" Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center"/>
    </Grid>
</Window>
